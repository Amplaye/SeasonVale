// Logica specifica per la pagina Help
if (is_active && visible) {
    // Qui implementeremo i controlli specifici per la pagina Help
    // Per ora solo un placeholder
    
    // Esempio: se premi ENTER quando questa pagina è attiva
    if (keyboard_check_pressed(vk_enter)) {
        show_debug_message("Help page activated!");
    }
}