// Logica specifica per la pagina Animals
if (is_active && visible) {
    // Qui implementeremo i controlli specifici per la pagina Animals
    // Per ora solo un placeholder
    
    // Esempio: se premi ENTER quando questa pagina è attiva
    if (keyboard_check_pressed(vk_enter)) {
        show_debug_message("Animals page activated!");
    }
}